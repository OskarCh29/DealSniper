CREATE TABLE car_deals (
id BIGSERIAL,
title VARCHAR(255) NOT NULL,
price DECIMAL(15,2) NOT NULL,
currency VARCHAR(10) NOT NULL,
offer_url VARCHAR(500) NOT NULL UNIQUE,
location VARCHAR(255) NOT NULL,
mileage VARCHAR(10),
year INT NOT NULL,
PRIMARY KEY(id)
);
